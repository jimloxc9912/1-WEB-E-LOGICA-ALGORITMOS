# Modulo 4
¡Bienvenido a **1-Web-js-logica-algoritmos**! Este proyecto está diseñado para documentar y practicar conceptos relacionados a logica y algoritmos. Aquí encontrarás apuntes teóricos, ejercicios prácticos y dinámicos, así como ejercicios guiados que se ejecutarán en cada entregable y dentro de cada clase. 

## Estructura del proyecto 
<details>
 <summary>Clase 1: Introducción a ECMAScript</summary>
 ECMAScript es el estándar de scripting que define cómo debe funcionar JavaScript. Es un conjunto de especificaciones publicadas por la organización Ecma International. 


  **1. Declaración de variables con let y const**
 
  **2. Arrow Functions**

  **3. Clases**
  * Permiten mejor estructuración.
  * Constructor: método especial para inicialización de un objeto.
  * Métodos: funciones definidad dentro de la clase.
  * Herencia (extends).

  **4. Módulos**
  * import y export(Métodos para exportar funciones o variables).
  
  **5. Promesas y manejo de asincroniá**
  * async/ await para manejo de codigo asíncrono. 
  * uso en peticiones HTTP en aplicaciones web y en aplicaciones del lado del servidor.

  **6.For..in**
  * Iterar sobre propiedades de un objeto.
  * Método hasOwnProperty(), solo propiedades propias.
  
  **7. For..of**
  * Iterar sobre valores de arreglos y otros objetos iterables(ejemplos:como una cadena de texto, un conjunto o un mapa).
  * No puede usarse directamente en obejtos.sugiere el uso de for..in o métodos Object.keys(), Object.values() o Object.entries().

  **8. ForEach()Iteración funcional(Arreglos)**
  * Itera sobre un arreglo y ejecuta una función para cada elemento.

  **10.map(): Iteración y Transformación (Arreglos)**
  * Itera sobre un arreglo y devuelve un nuevo arreglo con los resultados de la función.

</details>